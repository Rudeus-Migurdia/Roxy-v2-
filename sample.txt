<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gemini Style Sidebar</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- 引入 Lucide 图标库 -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap');
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: #131314;
            color: #e3e3e3;
        }

        .sidebar-transition {
            transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* 隐藏滚动条但保留滚动功能 */
        .no-scrollbar::-webkit-scrollbar {
            display: none;
        }
        .no-scrollbar {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .nav-item:hover {
            background-color: #282a2c;
        }

        .active-nav {
            background-color: #333537;
        }
    </style>
</head>
<body class="flex h-screen overflow-hidden">

    <!-- 侧边栏容器 -->
    <aside id="sidebar" class="sidebar-transition flex flex-col h-full bg-[#1e1f20] w-64 relative z-50">
        
        <!-- 顶部：菜单按钮 -->
        <div class="p-4 flex items-center">
            <button id="toggleBtn" class="p-2 rounded-full hover:bg-[#282a2c] transition-colors">
                <i data-lucide="menu" class="w-6 h-6"></i>
            </button>
        </div>

        <!-- 中部：新对话按钮 -->
        <div class="px-3 mb-4">
            <button class="flex items-center gap-3 px-4 py-3 rounded-full bg-[#1a1b1c] border border-gray-700 hover:bg-[#282a2c] transition-all w-fit min-w-[56px]">
                <i data-lucide="plus" class="w-6 h-6 text-blue-400"></i>
                <span class="sidebar-text font-medium whitespace-nowrap overflow-hidden">新对话</span>
            </button>
        </div>

        <!-- 底部：对话历史列表 -->
        <div class="flex-1 overflow-y-auto no-scrollbar px-3 space-y-1">
            <div class="sidebar-text text-xs font-semibold text-gray-400 px-4 py-2 uppercase">近期记录</div>
            
            <!-- 模拟历史项目 -->
            <a href="#" class="nav-item flex items-center gap-3 px-4 py-2 rounded-full active-nav">
                <i data-lucide="message-square" class="w-5 h-5 flex-shrink-0"></i>
                <span class="sidebar-text truncate">如何学习 React</span>
            </a>
            <a href="#" class="nav-item flex items-center gap-3 px-4 py-2 rounded-full">
                <i data-lucide="message-square" class="w-5 h-5 flex-shrink-0"></i>
                <span class="sidebar-text truncate">北京旅行攻略</span>
            </a>
            <a href="#" class="nav-item flex items-center gap-3 px-4 py-2 rounded-full">
                <i data-lucide="message-square" class="w-5 h-5 flex-shrink-0"></i>
                <span class="sidebar-text truncate">JavaScript 异步编程指南</span>
            </a>
        </div>

        <!-- 底部：固定设置 -->
        <div class="p-3 border-t border-gray-800 space-y-1">
            <button class="nav-item flex items-center gap-3 px-4 py-3 rounded-full w-full">
                <i data-lucide="help-circle" class="w-5 h-5 flex-shrink-0"></i>
                <span class="sidebar-text whitespace-nowrap">帮助</span>
            </button>
            <button class="nav-item flex items-center gap-3 px-4 py-3 rounded-full w-full">
                <i data-lucide="settings" class="w-5 h-5 flex-shrink-0"></i>
                <span class="sidebar-text whitespace-nowrap">设置</span>
            </button>
        </div>
    </aside>

    <!-- 主内容区域 -->
    <main class="flex-1 flex flex-col relative">
        <!-- 顶部导航栏 -->
        <header class="h-16 flex items-center justify-between px-6">
            <div class="flex items-center gap-2">
                <span class="text-xl font-medium">Gemini</span>
                <span class="text-xs bg-gray-800 px-2 py-0.5 rounded text-gray-400">Advanced</span>
            </div>
            <div class="w-8 h-8 rounded-full bg-gradient-to-tr from-blue-500 to-purple-500 cursor-pointer"></div>
        </header>

        <!-- 对话展示区 -->
        <div class="flex-1 flex flex-col items-center justify-center p-4">
            <h1 class="text-4xl font-semibold mb-8 text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-red-400">
                有什么可以帮您的吗？
            </h1>
            
            <!-- 输入框容器 -->
            <div class="w-full max-w-3xl bg-[#1e1f20] rounded-2xl p-4 shadow-xl border border-transparent focus-within:border-gray-600 transition-all">
                <textarea 
                    placeholder="在这里输入提示词" 
                    class="w-full bg-transparent border-none outline-none resize-none text-lg placeholder-gray-500"
                    rows="1"></textarea>
                <div class="flex justify-between items-center mt-4">
                    <div class="flex gap-2">
                        <button class="p-2 hover:bg-[#282a2c] rounded-full text-gray-400"><i data-lucide="image" class="w-5 h-5"></i></button>
                        <button class="p-2 hover:bg-[#282a2c] rounded-full text-gray-400"><i data-lucide="mic" class="w-5 h-5"></i></button>
                    </div>
                    <button class="p-2 bg-gray-700 rounded-full text-gray-500 cursor-not-allowed">
                        <i data-lucide="arrow-up" class="w-5 h-5"></i>
                    </button>
                </div>
            </div>
        </div>
    </main>

    <script>
        // 初始化图标
        lucide.createIcons();

        const sidebar = document.getElementById('sidebar');
        const toggleBtn = document.getElementById('toggleBtn');
        const sidebarTexts = document.querySelectorAll('.sidebar-text');
        
        let isCollapsed = false;

        toggleBtn.addEventListener('click', () => {
            isCollapsed = !isCollapsed;
            
            if (isCollapsed) {
                // 折叠状态
                sidebar.classList.remove('w-64');
                sidebar.classList.add('w-[72px]');
                sidebarTexts.forEach(text => text.classList.add('hidden'));
            } else {
                // 展开状态
                sidebar.classList.remove('w-[72px]');
                sidebar.classList.add('w-64');
                // 延迟显示文字，等待宽度动画
                setTimeout(() => {
                    if(!isCollapsed) sidebarTexts.forEach(text => text.classList.remove('hidden'));
                }, 100);
            }
        });
    </script>
</body>
</html>